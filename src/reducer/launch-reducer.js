
const initialState = {
    launchListResponse: undefined
}
export default function launches(state = initialState, action = '') {
    switch (action.type) {
        default:
            return state
    }
}